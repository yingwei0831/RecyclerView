<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Animation.View" parent="AppTheme"/>

    <style name="Theme_AudioDialog">
        <item name="android:windowBackground">@android:color/transparent</item> <!-- 提示框的背景颜色 -->
        <item name="android:windowFrame">@null</item>           <!-- 提示框是否有边框 -->
        <item name="android:windowIsTranslucent">true</item>    <!-- 提示框是否是透明 -->
        <item name="android:windowIsFloating">true</item>       <!-- 提示框是否是浮动 -->
        <item name="android:backgroundDimEnabled">false</item>  <!-- 是否充许对话框的背景变暗 -->
    </style>
        <!--<item name="android:windowNoTitle">true</item>          &lt;!&ndash; 提示框是否有标题 &ndash;&gt;-->
</resources>
